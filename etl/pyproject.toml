[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q --strict-markers"
markers = [
    "unit: pure-function tests, no database",
    "integration: needs a Postgres instance (testcontainers or TEST_DATABASE_URL)",
]

[tool.ruff]
line-length = 100
target-version = "py312"
